document.addEventListener('DOMContentLoaded',()=>{const buttons=[...document.querySelectorAll('[data-filter-value]')];const cards=[...document.querySelectorAll('.slide-card[data-filter]')];for(const button of buttons){button.addEventListener('click',()=>{const value=button.dataset.filterValue;for(const item of buttons)item.setAttribute('aria-pressed',String(item===button));for(const card of cards)card.hidden=value!=='all'&&card.dataset.filter!==value;});}});
