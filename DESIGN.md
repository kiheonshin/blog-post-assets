# Kiheon Blog 디자인 시스템

## 1. 분위기와 정체성

신기헌의 생각과 경험에서 출발한 글과 원자료가 주제별 시리즈로 쌓이는 개인 블로그다. 오래된 종이 위에 현재의 링크와 기록을 정확히 얹은 편집 아카이브의 인상을 유지하되, 최상위 정체성은 시리즈 보관함이 아니라 한 사람의 기록이 이어지는 블로그로 둔다. 대표적인 시각 장치는 한 장의 파노라마 위에 개별 글의 좌표를 겹쳐 놓는 `Series Navigator`이며, 장식보다 제목·순서·현재 위치를 읽는 일이 먼저다.

## 2. 색상

| 역할 | 토큰 | 값 | 사용 |
|---|---|---:|---|
| 캔버스 | `--canvas` | `#F3F3EF` | 전체 배경 |
| 표면 | `--surface` | `#FBFBF8` | 카드와 포커스 표면 |
| 반투명 표면 | `--surface-overlay` | `rgba(243,243,239,.94)` | 이미지 위 내비게이션 |
| 본문 | `--ink` | `#17181A` | 제목·본문 |
| 보조 본문 | `--ink-muted` | `#5E6065` | 설명·날짜 |
| 약한 본문 | `--ink-faint` | `#686A6F` | 비활성·메타 |
| 구분선 | `--rule` | `#CFCFC8` | 기본 선 |
| 강한 구분선 | `--rule-strong` | `#17181A` | 현재 위치·강조 |
| 링크 | `--accent` | `#2457D6` | 이동 가능한 요소 |
| 링크 호버 | `--accent-hover` | `#163DA7` | 호버·활성 |
| 역상 표면 | `--inverse` | `#17181A` | 사이트 푸터 |
| 역상 본문 | `--inverse-ink` | `#F7F7F2` | 사이트 푸터 본문 |

색상은 정보 역할을 가진다. 파랑은 링크와 포커스에만 사용하고 장식에는 사용하지 않는다.

### 2.1 기능 아이콘

- 공용 자산은 assets/icons/klu-interface-icons.svg 한 파일로 관리한다.
- 기본 기하는 24×24, 실제 UI 렌더는 16×16, 선 굵기는 1.75, 끝과 모서리는 round다.
- 색은 SVG 안에 고정하지 않고 currentColor로 기능 토큰을 상속한다.
- v1 심볼은 arrow-right, external-link, search, lock 네 가지다.
- 아이콘은 이동, 검색, 보호처럼 행동이나 상태를 설명할 때만 사용한다. 섹션 제목과 장식에는 사용하지 않는다.
- 아이콘만 보이는 컨트롤은 32×32 이상을 확보하고 숨은 텍스트로 기능 이름을 제공한다.
- 같은 기능의 화살표 글리프나 이모지를 새로 만들지 않는다. 정적 공개면은 외부 심볼 참조 대신 공용 자산과 같은 path를 인라인으로 출력한다.

## 3. 타이포그래피

| 수준 | 크기 | 굵기 | 행간 | 사용 |
|---|---:|---:|---:|---|
| Display | `clamp(2.1rem,3.6vw,3.3rem)` | 400 | 1.15 | 페이지 제목 |
| H2 | `clamp(1.45rem,2vw,1.85rem)` | 400 | 1.3 | 본문 절 제목 |
| Body large | `clamp(1.06rem,1.5vw,1.22rem)` | 400 | 1.62 | 리드 |
| Body | `1.0625rem` | 400 | 1.78 | 본문 |
| Card title | `.9375rem` | 600 | 1.3 | 포스트·자료 카드 |
| Small (`--small`) | `.875rem` | 400 | 1.6 | 설명·캡션 |
| Meta | `.75rem` | 500 | 1.4 | 날짜·헤더 |
| Micro | `.6875rem` | 600 | 1.3 | 순번·오버라인 |

- Display: `"Iowan Old Style", "Noto Serif CJK KR", "AppleMyungjo", "Nanum Myeongjo", serif`
- Body: `-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", sans-serif`
- Mono: `"SFMono-Regular", "Roboto Mono", "Noto Sans Mono CJK KR", monospace`
- 한국어는 `word-break: keep-all`, 제목은 `text-wrap: balance`, 본문은 `text-wrap: pretty`를 기본으로 한다.

## 4. 간격과 레이아웃

기본 단위는 4px이다.

| 토큰 | 값 | 사용 |
|---|---:|---|
| `--space-1` | `.25rem` | 미세 간격 |
| `--space-2` | `.5rem` | 인라인·조밀한 목록 |
| `--space-3` | `.75rem` | 메타 행 |
| `--space-4` | `1rem` | 모바일 거터·카드 패딩 |
| `--space-6` | `1.5rem` | 카드 그룹 |
| `--space-8` | `2rem` | 태블릿 거터 |
| `--space-10` | `2.5rem` | 주요 블록 |
| `--space-12` | `3rem` | 데스크톱 거터 |
| `--space-16` | `4rem` | 페이지 구획 |
| `--space-20` | `5rem` | 푸터 전 간격 |

- 읽기 폭: `46rem`
- 발화 기록 읽기 폭: `42rem`
- 시리즈·라이브러리 폭: `66rem`
- 전체 헤더 폭: `90rem`
- 브레이크포인트: 48rem, 70rem, 80rem
- URL 계층: `/`(라이브러리) → `/series/{series-slug}/`(시리즈) → `/series/{series-slug}/posts/{post-slug}/`(포스트), `/archive/`(원자료 모듈과 해석층) → `/archive/world-atlas/`(공개 참고면)
- 각 시리즈의 이미지·원자료는 해당 시리즈 디렉터리 아래에 둔다.
- 아카이브 연도는 `발행일`과 `원자료 연도`를 분리해 저장하고, 독자가 연도로 찾을 때는 두 축을 함께 검색한다.
- `topics`는 여러 글을 잇는 통제 어휘, `keywords`는 글 안의 구체적인 개념어로 사용한다.
- 메인 화면의 설명과 표지는 역할이 겹치지 않게 나눈다.
  1. 최상단 리드는 블로그 전체의 기록 범위와 세 탐색 경로를 설명한다.
  2. 홈페이지는 Series, Explore, KLU 구조 순서로 읽고 레거시 구현명은 새 표시 이름으로 쓰지 않는다.
  3. 실제 콘텐츠 구획은 설명 문장이나 같은 번호·제목을 반복하지 않고 `SERIES`·`EXPLORE` 편집 표식과 강한 선으로 시작한다.
  4. Explore 상태 문구는 현재 필터·전체 결과 수·현재 표시 수·정렬만 알린다.

## 5. 컴포넌트

### Site Header

- **구조**: `header > .site-header__in > brand + context`
- **상태**: 기본, 호버, 키보드 포커스
- **접근성**: 첫 링크 앞에 본문 건너뛰기 링크를 둔다.
- **레이아웃**: 양끝 Cluster

### Series Library

- **구조**: `series-library > status + ol.library-grid + nav.series-pager`
- **변형**: 공개 시리즈 6개 이하, 7개 이상
- **상태**: 기본, 호버, 포커스, 이전 페이지, 다음 페이지, 처음·끝 비활성
- **접근성**: 카드 전체가 하나의 명확한 링크이며 제목과 글 개수를 텍스트로 제공한다.
- **레이아웃**: 첫 화면에 여섯 개의 완전한 카드를 둔다. 모든 페이지에서 썸네일·시리즈 번호·기간·포스팅/자료 수·제목·설명·행동을 동일하게 보존한다.
- **확장 규칙**: manifest 순서를 편집 순서로 존중하고 이전·다음이 현재 세 장을 교체한다. 추가 시리즈가 아래로 펼쳐져 Explore를 미는 구조는 사용하지 않는다.
- **요약**: `1–6 / 전체` 범위, 얇은 페이지 세그먼트, 다음 화면에 남은 수를 한 레일에서 제공한다. 큰 이전·다음 텍스트 버튼은 사용하지 않는다.

### Homepage Reading Order

- **구조**: `library-head → section#series → section#archive → section.universe-map`.
- **머리**: `Blog` 메타와 H1만 둔다. 긴 소개 문단과 별도 3분할 탐색 줄은 Universe Map과 역할이 겹치므로 사용하지 않는다.
- **첫 콘텐츠**: 페이지 표제 다음에 Series 목록이 바로 시작한다.
- **탐색**: Series 다음의 Explore가 공개 글과 원자료를 검색·필터·정렬한다.
- **마지막 콘텐츠**: `KIHEON LIFE UNIVERSE` 프로젝트 소개와 전체 제품 라인업을 페이지 끝에 둔다.

### Homepage Universe Map

- **위치**: Explore 뒤, 홈페이지의 마지막 주 콘텐츠로 한 번만 둔다. 개별 포스트에는 반복하지 않는다.
- **구조**: `section.universe-map > header + figure.klu-system-map + product label + ul.universe-map__surfaces + note`.
- **프로젝트 명시**: `KIHEON LIFE UNIVERSE`는 큰 제목이 아니라 작은 `ECOSYSTEM MAP` 좌표로 둔다. 주 제목은 「기록에서 제품까지 이어지는 전체 구조」라는 한국어 주장이다.
- **시스템 도식**: 의미 관계는 `hierarchy`로 검증하되 화면은 `instrument-grid-v1`로 렌더한다. 1px 프레임·희미한 28px 그리드·모노 좌표·원형 상태점·검증의 막을 사용하고 방향 화살표는 금지한다. KLU 아래 격리 보존, 시간·공간·관계, 브랜드, 공개 제품, 에이전트·도구, 검증·발행과 여섯 공개 표면을 13노드·12 의미 연결로 관리한다.
- **검증**: JSON 스펙, 결정론적 렌더러, SVG, receipt를 함께 보존한다. receipt는 bounds PASS, overlaps 0, warnings 0, 금지 표시어 0을 요구한다. 작은 화면에서는 글자를 축소하지 않고 도식 캔버스를 좌우로 이동한다.
- **표시 용어**: `세계 모델`은 본인 확정이 아닌 2026-08-30 에이전트 후보 표현이라 폐기하고 `시간·공간·관계`를 쓴다. `World Atlas`는 레거시 경로·코드에만 남기며 새 표면에는 `Outside`를 쓴다. 부모 `Life World`도 이 공개 요약면에서는 표시하지 않고 `Outside / Inside`를 직접 병렬 표기한다.
- **Archive 구분**: 로컬의 비공개 원본 영역은 `LOCAL VAULT · 격리 보존`, 웹 공개 제품은 `ARCHIVE · 공개 근거·자료`로 분리한다.
- **제품 순서**: `Blog → Archive → Universe Map → Outside → Inside → Slides`. Outside와 Inside는 같은 단계의 병렬 표면이다.
- **열람 가능**: Blog, Archive, Outside, Universe Map은 실제 `<a>`다. 행동 문구와 `열람 가능` 상태를 함께 제공하고 키보드 포커스·호버를 지원한다.
- **열람 불가**: Inside와 Slides는 `<article>`이며 링크·`href`·클릭 동작이 없다. 파선 테두리, 빈 상태점, `링크 없음`과 `아직 공개되지 않음`을 함께 표시한다. 색만으로 차이를 알리지 않는다.
- **레이아웃**: 68rem 이상 3열, 42rem 이상 2열, 그 아래 1열. 시스템 맵은 넓은 캔버스를 유지하고 좁은 화면에서 가로 이동을 허용한다.
- **규격 잠금**: `tests/home-universe-map.test.mjs`가 Series·Explore 뒤 배치, SVG 안전성·receipt, 공개 4개 링크, 비공개 2개 비링크, 제품 순서·경로와 포스트 미반복을 검사한다.

### Archive Material Inventory

- **위치**: Archive의 네 원자료 모듈 카드 바로 뒤에 둔다. Outside·Inside 제품 소개는 홈의 Universe Map이 소유하므로 Archive에서 반복하지 않는다.
- **구조**: `section.arc-stock > heading + description + dl > div[data-access]`.
- **역할**: 공개 페이지가 있는 자료와 원본만 보존하는 자료를 한 인벤토리에서 함께 기록한다. 링크가 없는 항목도 삭제하지 않아 자료의 존재와 규모를 보존한다.
- **열람 가능**: 7개 항목은 제목을 실제 `<a>`로 만들고 `열람 가능`을 표시한다.
- **원본만 보존**: 2개 항목은 제목을 일반 텍스트로 두며 `href`나 클릭 동작 없이 `원본만 보존`을 표시한다.
- **접근성**: 링크 여부는 파란색만으로 구분하지 않고 실제 요소 종류와 상태 텍스트를 함께 사용한다.
- **규격 잠금**: Archive 홈 테스트가 Outside·Inside 소개 부재, 7개 링크·2개 비링크, 상태 문구와 참조 무결성을 검사한다.

### Outside Reference Surface

- **구조**: 레거시 경로 `archive/world-atlas/`의 Zones → Objects → Materials → Relations → Boundaries 다섯 구획
- **데이터**: 공개 안전 JSON 하나를 단일 문맥으로 읽고 `window.worldAtlasRead`의 다섯 읽기 함수만 제공한다. 기존 manifest와 시리즈 내비게이션에는 연결하지 않는다.
- **상호작용**: 구역 5개와 관계 5개는 서로 독립된 tablist다. 포인터와 좌우 방향키, Home, End, roving tabindex, 선택 뒤 포커스 유지를 지원한다.
- **경계**: 표시 이름은 Outside다. 레거시 경로·코드명은 호환을 위해 유지한다. `/universe/world/`는 향후 경험을 설명하는 텍스트일 뿐 링크가 아니며, 방향·거리·중심·위계를 시각 배치에서 추론하지 않는다.
- **실패 상태**: 문맥을 불러오지 못하면 읽기 어댑터와 탭을 만들지 않고, 보이는 `role=alert`에서 오류를 알린다.

### Library Section Marker

- **구조**: `section.library-section > h2.library-section__marker + content`
- **역할**: 상단 탐색 경로의 이름과 설명을 반복하지 않으면서 시리즈와 아카이브의 실제 시작점을 표시한다.
- **시각 위계**: 강한 위쪽 선과 파란 모노 `SERIES`·`EXPLORE` 표식만 사용한다. `Archive`는 독립 제품 이름이므로 검색·필터 영역의 이름으로 재사용하지 않는다.
- **레이아웃**: 모든 화면 크기에서 한 줄 편집 표식 뒤에 실제 콘텐츠가 바로 시작된다.
- **접근성**: H2의 접근 가능한 이름은 각각 `시리즈로 읽기`, `기록 탐색`으로 유지하고, 화면에는 짧은 영문 표식만 보여준다.

### Explore Results

- **구조**: `archive-library > form.archive-controls + status + ol.archive-results + pagination`
- **데이터**: `content-manifest.js`의 공개 포스팅과 공개 원자료만 색인한다. `sourceModuleLinks`의 비공개 목록 정책은 유지한다.
- **메타데이터**: 자료 유형, 발행일, 원자료 연도, 주제(`topics`), 키워드(`keywords`), 시리즈를 사용한다.
- **상태**: 전체, 검색 중, 필터 적용, 결과 없음, 초기화, 이전 페이지, 다음 페이지, 처음·끝 비활성
- **정렬**: 최신 발행순, 오래된 발행순, 시리즈순
- **URL 상태**: `q`, `type`, `year`, `topic`, `tag`, `sort`, `page`를 쿼리 문자열에 보존해 같은 탐색 결과를 다시 열 수 있게 한다.
- **레이아웃**: Explore 전체를 하나의 외곽 프레임으로 묶는다. `DISCOVERY CONTROLS`는 내부 상단 패널이고 결과 상태·목록·페이지 레일은 같은 프레임 안의 다음 구획이다. `SEARCH RESULTS`를 독립 섹션 표식으로 사용하지 않는다. 한 페이지의 8건만 교체해 아래 내용을 밀지 않는다.
- **결과 위계**: 자료 유형·시리즈·발행일·원자료 연도를 첫 줄에 가로로 노출하고, 제목은 본문 산세리프 1rem으로 제한한다. 주제·키워드를 제목 바로 아래에 둔다. 설명은 검색 색인에는 포함하지만 결과 카드에서는 반복하지 않는다.
- **상태 문구**: 전체 결과 수를 가장 먼저 두고, 적용된 필터 범위와 정렬을 뒤에 붙인다. 목록 아래에는 현재/전체 페이지와 현재 범위(`1–8/32`)를 보인다.
- **접근성**: 모든 입력에 가시적 레이블을 제공하고, 결과 수·필터 범위·정렬·페이지 진행은 `aria-live="polite"`로 알린다. 이전·다음은 결과 목록을 `aria-controls`로 가리키며 필터가 바뀌면 1페이지로 돌아간다.

### Content Facets

- **구조**: `content-facets > dl > (연도 | 주제 | 키워드)`
- **데이터**: 현재 포스팅 또는 원자료의 manifest 메타데이터를 읽는다.
- **상태**: 연도·주제·키워드 링크, 데이터 없음
- **범위**: 비공개 원자료 페이지에서는 해당 페이지 자체를 공개 목록에 노출하지 않고, 같은 메타데이터를 가진 공개 항목으로 이동하는 입구로만 쓴다.
- **접근성**: 링크 문구만으로 필터 기준을 이해할 수 있어야 한다.

### Series Navigator

- **구조**: `series-nav > figure.banner > image + ol.banner__cards`
- **변형**: 시리즈 홈, 현재 글, 4편 이상
- **상태**: 이동 가능, 현재 글(`aria-current="page"`), 호버, 포커스
- **간격**: `--space-2`, `--space-3`, `--space-4`
- **높이 규칙**: 같은 행의 모든 카드는 가장 긴 카드 높이에 맞춘다. 제목 영역은 최소 두 줄 높이를 확보한다.
- **확장 규칙**: 3편 이하는 파노라마 위에 겹치고, 4편 이상은 이미지 아래 자동 맞춤 그리드로 내려온다.
- **접근성**: `nav`와 순서 목록을 사용하고 현재 글은 링크가 아닌 상태 카드로 표시한다.
- **모션**: 150ms 색상·테두리 변화만 허용한다.

### Source Card

- **구조**: 자료 종류 + 제목 + 날짜·설명
- **상태**: 기본, 호버, 포커스
- **레이아웃**: 두 칸 Grid, 모바일 한 칸

### Source Page Frame

- **구조**: `body.library-page.source-page > site header + main > .wrap > .source-head + content + site footer`
- **레이아웃**: 메인 인덱스와 같은 `--wide` 콘텐츠 폭과 `--measure` 읽기 폭을 쓴다.
- **타이포그래피**: 메타는 모노, 제목은 Display, 리드는 Body large를 쓴다.
- **구획**: 원자료 모듈과 함께 보기의 제목은 `library-section__label` 문법으로 표시한다.
- **접근성**: 링크와 상호작용 버튼 모두 같은 파란 2px 키보드 포커스 윤곽을 쓴다.

### Source Module Links

- **구조**: `source-module-links > nav > ul.sources > li > a`
- **데이터**: 원자료 모듈 사이의 이동만 `content-manifest.js`의 `sourceModuleLinks`에서 읽는다.
- **범위**: 해당 원자료 페이지 안에서만 쓰며, 공개 시리즈·자료 목록에는 이 데이터를 섞지 않는다.
- **접근성**: `nav`에 맥락을 드러내는 레이블을 주고, 현재 페이지는 목록에서 제외한다.

### Series Post Links

- **구조**: `series-post-links > nav.serieslinks > link | current`
- **변형**: 포스트의 현재 글 표시, 원자료의 전체 글 링크
- **데이터**: `content-manifest.js`의 포스트 순서·제목·주소를 그대로 사용한다.
- **접근성**: 현재 글은 링크가 아닌 `aria-current="page"` 상태로 표시한다.

### Series Docent

- **구조**: `<kiheon-voice-assistant>` 하나가 시리즈의 전체 rail과 상세 페이지의 compact entry를 함께 제공한다.
- **시리즈 배치**: DOM에서는 `<series-nav>` 바로 뒤, `<series-sources>`보다 앞에 둔다. 70rem 이상에서는 17rem 오른쪽 rail, 그 아래에서는 같은 DOM 순서의 한 열 블록이다.
- **좁은 화면**: 48rem 이하에서는 시리즈 rail의 제목·설명·`열기`만 먼저 보이고 나머지는 사용자가 펼친다. 상세 진입 바는 문서 흐름 안에 머물며 sticky로 고정하지 않는다.
- **상세 배치**: 포스트는 lead 다음, 원자료는 source header 다음에 진입 바를 둔다. 열린 패널은 overlay 또는 bottom sheet이므로 본문과 목차의 계산 폭을 바꾸지 않는다.
- **상태**: 준비, 듣기, 답 준비, 답하기, 오류. 질문 버튼을 누르기 전에는 마이크와 음성 안내를 시작하지 않는다.
- **접근성**: 대화 기록은 항상 패널 안에 보이며 polite live region을 사용한다. Escape로 상세 패널을 닫고 진입 버튼으로 포커스를 돌려보내며, 모바일 safe area와 reduced motion을 존중한다.
- **시각 규칙**: 기존 종이색과 1px 선을 사용한다. 그림자, 둥근 모서리, 블러를 추가하지 않는다.
- **토큰 규칙**: 간격과 글자 크기는 `--space-*`, `--small`, `--meta`, `--micro`를 그대로 상속한다. 독립 인라인 스타일을 쓰는 원자료 페이지에서는 같은 값의 fallback만 허용한다.
- **기하 규칙**: transcript 높이는 `--space-12` 두 배에서 `--space-16` 세 배, 하단 시트 높이는 viewport에서 `--space-16`을 뺀 값, 데스크톱 패널 폭은 `--measure`의 절반을 쓴다. 반응형 경계는 사이트 공통 48/70/80rem만 사용한다.
- **색 규칙**: overlay scrim은 별도 RGB를 만들지 않고 `--ink`와 transparent의 22% 혼합으로 만든다.

### Transcript Reader

- **구조**: `transcript-reader > aside.transcript-index + div.transcript-reader__body > section.talk > section.tr__segment`
- **원문 보존**: 발화 문장은 수정하지 않는다. 원본의 임의 문단 경계만 브라우저에서 문장 단위로 다시 묶는다.
- **문단 규칙**: 짧은 문장은 한 읽기 덩어리로 합치고, 긴 문단은 문장 경계에서 나눈다. 정정 표시는 해당 근거 바로 앞의 덩어리에 남긴다.
- **탐색**: 발표 2건과 세부 구간 18개를 한 목차에서 제공하고, 각 구간에 안정적인 앵커를 부여한다.
- **정정 근거**: 본문 흐름을 끊지 않도록 `details`로 접되, 생략·마스킹 표시는 항상 보이게 둔다.
- **레이아웃**: 70rem 이상에서는 목차와 읽기 열을 나란히 두고 목차를 sticky로 둔다. 그보다 좁은 화면에서는 목차를 접을 수 있는 한 열 흐름으로 바꾼다.
- **접근성**: 목차는 실제 링크 목록, 정정 근거는 `summary`, 현재 해시는 네이티브 앵커 이동으로 동작한다. JavaScript가 꺼져도 원문 전체를 읽을 수 있어야 한다.

### Legacy Redirect

- **구조**: canonical + 즉시 이동 + 수동 링크
- **접근성**: 자바스크립트가 꺼져도 새 주소로 이동할 수 있는 링크를 제공한다.

## 6. 모션과 상호작용

| 유형 | 시간 | 이징 | 사용 |
|---|---:|---|---|
| Micro | 150ms | ease-out | 링크·카드 테두리와 색상 |

- 이동·크기 애니메이션은 사용하지 않는다.
- `prefers-reduced-motion: reduce`에서는 전환을 제거한다.
- 호버는 링크에만 적용하며 현재 글 카드에는 적용하지 않는다.

## 7. 깊이와 표면

전략은 `borders-only`다. 그림자와 블러를 사용하지 않는다. 종이색 표면, 1px 선, 이미지와의 겹침만으로 층위를 만든다.

## 8. 접근성 제약과 허용된 부채

### 제약

- 목표: WCAG 2.2 AA
- 본문 대비 4.5:1 이상, 큰 글자와 UI 경계 3:1 이상
- 모든 링크에 가시적 포커스 표시
- 375px에서 가로 스크롤 없이 한 열로 전환
- 200% 확대에서도 아카이브 필터와 발화 목차가 내용과 겹치지 않는다.
- 의미 있는 이미지는 대체 텍스트와 고정 크기를 갖는다.
- 시리즈 카드 제목은 한국어 어절 중간에서 끊지 않는다.

### 허용된 부채

| 항목 | 위치 | 이유 | 종료 조건 |
|---|---|---|---|
| 장문 페이지의 인라인 CSS | 기존 포스트·원자료 HTML | 이번 변경은 URL 계층과 공통 시리즈 내비게이션에 한정한다. | 다음 콘텐츠 템플릿화 단계에서 공통 CSS로 이동 |
| 기존 GA4 헤드 스크립트 | 공개 페이지 전체 | 기존 측정 체계를 유지한다. | 개인정보·쿠키 정책을 별도로 정리할 때 재검토 |
